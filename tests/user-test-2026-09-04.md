# Antra UX Test — 2026-09-04

## Phase 1: Discovery (Website)

- **What does this tool do?** A local dev proxy that gives your apps stable HTTPS domains (like `myapp.localhost`) without ports, hosts file edits, or cert warnings.
- **What's the promise?** "One command" to get a real HTTPS URL for local dev. No config headaches.
- **Does the website make you want to try it?** Yes. The problem statement resonates (localhost:5173 is annoying), and the comparison table clearly shows advantages over alternatives.
- **Confusing/unclear:** Nothing immediately confusing. Architecture diagram is clear and helpful.
- **Questions before installing:** What permissions does it need? Is the curl one-liner safe?

## Phase 2: Installation

- **How smooth was the install?** Antra was already installed (`antra 0.1.0`). The install script URL on the website (`https://antra.iifelse.com/install.sh`) returns **HTML instead of a shell script** — broken link. The actual script lives at `https://raw.githubusercontent.com/ifelse-codes/antra/main/install.sh`.
- **Did you feel confident it was safe to run?** N/A — already installed. The install script itself looks clean (checksum verification, local-only CA).
- **Any unnecessary/confusing steps?** The broken install URL is a blocker for new users.

## Phase 3: First Run

- **`--help` output:** Clear, concise. Lists all 9 commands. Enough to get started.
- **`doctor` output:** Uses ✓/✗/⚠ symbols. Tells you exactly what's wrong and how to fix it. Very good.
- **Could you figure out next steps?** Yes — `antra trust` then `antra proxy start` is obvious.

## Phase 4: Core Usage

- **`antra alias`:** Worked first try. Clear output with domain → port mapping.
- **`antra list`:** Clean table format. Shows domain, port, PID, uptime.
- **`antra run --domain ... --port ... -- command`:** Worked with explicit `--port`. Without `--port`, it auto-detects (picked a random port 62041 for a server on 9090 — confusing mismatch).
- **HTTPS access:** `curl -sk https://test.localhost` returned 200. Real HTTPS working.
- **`antra proxy status`:** Shows PID, uptime, route count, socket path.
- **Output feel:** Clear and well-formatted. Good use of symbols and colors.

### Issue: `antra run` without `--port` auto-detection is unreliable

When running `antra run --domain myapp.localhost -- python3 -m http.server 9090`, antra registered the route as `127.0.0.1:62041` instead of `9090`. The user must pass `--port` explicitly or the proxy points to the wrong port. The help text says "auto-detected if omitted" but it doesn't actually detect the port from the command.

## Phase 5: Error Paths

| Test | Result |
|------|--------|
| Port already in use | OS-level error — clear enough |
| Domain with no route | Returns **200** with content (the Antra website itself). No "no route" error. **Confusing.** |
| `proxy stop` when not running | Clear message: "Daemon is not running" ✓ |
| `antra clean` | Prompts for confirmation, removes everything cleanly ✓ |
| `antra remove` | Works, shows before/after route list ✓ |

### Issue: No route returns 200 instead of an error

Accessing `https://nonexistent.localhost` when the daemon has no matching route returns a 200 with what appears to be the Antra website content. There's no clear "no route found" or 502 error. This is confusing — users won't know if their app is working or if something else is being served.

## Phase 6: Comparison

| Question | Answer |
|----------|--------|
| On a scale of 1-10, how easy was this to set up? | **8** — Once installed, very smooth. Install URL broken (-1), port auto-detect unreliable (-1). |
| On a scale of 1-10, how well does it deliver on its promise? | **8** — Real HTTPS works, no hosts edits, clean URLs. Port detection and no-route behavior need work. |
| Would you use this regularly? | Yes — the core value prop is strong. Beats `localhost:5173` and ngrok for local dev. |
| What's the #1 thing that would make you stop using it? | If it breaks on macOS updates (port 443/80 conflicts with AirPlay or other services). |
| What's the #1 thing that would make you recommend it? | If `antra run` "just works" without needing `--port` — true one-command UX. |
| What surprised you (good)? | How fast the daemon starts. The `doctor` command is excellent. |
| What surprised you (bad)? | Broken install URL. No-route returns 200. |
| What feature is missing? | `antra logs` — see what traffic is flowing through the proxy. |
| How does this compare to localhost:port? | Much better — real HTTPS, stable domains, cookies work. |
| How does this compare to ngrok? | Better for local dev (no cloud, no account, stable domain). Ngrok still better for sharing with others. |
| Any final feedback? | Fix the install URL, handle no-route gracefully, and `antra run` should auto-detect port reliably. |

## Phase 7: Specific Promises Check

| Promise | Delivers? | Evidence |
|---------|-----------|----------|
| "One command" | ⚠️ Partial | Works but requires `--port` for reliable routing. Without it, wrong port is used. |
| "Real HTTPS" | ✅ Yes | `curl -sk https://test.localhost` returns 200. No cert warnings after trust. |
| "No ports" | ✅ Yes | `https://myapp.localhost` — no `:5173` in URL. |
| "No /etc/hosts" | ✅ Yes | `.localhost` resolves via mDNS/default. No hosts edits needed. |
| "No certificate warnings" | ✅ Yes | After `antra trust`, HTTPS works cleanly. |
| "WebSocket/HMR support" | ⚠️ Untested | Cannot verify from CLI alone — need a real Vite/Next.js app to test HMR. |
| "Language-agnostic" | ✅ Yes | Works with `python3 -m http.server`. Any process. |
| "No cloud, no accounts, no telemetry" | ✅ Yes | Fully local. No sign-up, no cloud calls. |
| "Multi-app routing" | ✅ Yes | Two routes (`test.localhost`, `myapp.localhost`) ran simultaneously. |
| "Project config" | ⚠️ Untested | `antra.toml` not tested — would need a real project setup. |
